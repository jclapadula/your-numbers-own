{
  "name": "your-numbers-own",
  "private": true,
  "scripts": {
    "dev": "concurrently 'cd frontend && bun dev' 'cd backend && bun dev'",
    "ngrok": "ngrok http --url=untraceried-stephany-nonpolitically.ngrok-free.app 8080",
    "check": "concurrently 'cd frontend && bun run check' 'cd backend && bun run check'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
